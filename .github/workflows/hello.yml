name: Hello Agile
on:
  push:
    branches:
      - [cite_start]dev  # Triggers on push to dev [cite: 72]
  pull_request:
    branches:
      - [cite_start]main # Triggers on PR targeting main [cite: 72]
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"